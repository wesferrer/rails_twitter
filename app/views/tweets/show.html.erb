<h1> <%= @tweet.handle %> </h1>

<p> <%= @tweet.content %> </p>
<br>
<%= @tweet.created_at.localtime.strftime(" %I:%M %P %b, %-d, %Y") %>
<br>

<%= link_to "Edit", edit_tweet_path(@tweet)%>
<%= link_to "Delete", tweets_path, method: :delete, data: { confirm: "Are you sure you want to delete this tweet?"}%>
<%= link_to "Home", tweets_path %>
